version: "4"
services:
  indomie:
    build: .
    environment:
      BOT_TOKEN: $BOT_TOKEN
      BOTLOG_CHATID: $BOTLOG_CHATID
      STRING: $STRING
      API_KEY: $API_KEY
      API_HASH: $API_HASH
      HEROKU_API_KEY: $HEROKU_API_KEY
      HEROKU_APP_NAME: $HEROKU_APP_NAME
